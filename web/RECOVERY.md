# Player website recovery — 14 September 2026

The original workspace became unavailable after local commit `7743be41022b8acabbed3ded9a61a62e840c8bb6`. That commit never reached GitHub. Source is being recovered from the conversation onto `feat/player-facing-website` with explicit owner authorization.

This is reconstructed source, not a byte-identical recovery. The original generated Lombardy PNG, dependency lockfile, compiled assets and private preview checkout could not be retrieved. The hero supports an optional image and renders as a deliberate typographic surface without one.

The earlier workspace passed frontend/backend builds, eight client tests and in-memory callable checks. Those results apply only to the lost original commit. The reconstructed source requires fresh validation; do not report those earlier passes as tests of this branch.

No Firebase production deployment or Sites version was published. The reserved private Site is `appgprj_6aa7cba16fa48191a1507576d347e34b`. Reuse that identity if publication resumes; do not create a duplicate.

Preserve the replay statistics contract and distinguish planned, inferred and validated capabilities. Replay upload, drafting and statistics-dependent progression remain unfinished.
